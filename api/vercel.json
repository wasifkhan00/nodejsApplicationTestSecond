{
    "rewrites": [
      { "source": "/(.*)", "destination": "/api/server.js" }
       ],

     "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/server.js"  // Routes all API requests to the server.js
    },
  }
